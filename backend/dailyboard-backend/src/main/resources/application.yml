spring:
  application:
    name: daily-board
  lifecycle:
    timeout-per-shutdown-phase: "5s"
  servlet:
    multipart:
      max-file-size: 512KB
  datasource:
    url: jdbc:mysql://localhost:3306/daily-board?useSSL=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Europe/Warsaw&autoReconnect=true&characterEncoding=UTF-8&useUnicode=yes&jdbcCompliantTruncation=false&sessionVariables=sql_mode='NO_ENGINE_SUBSTITUTION'
    username: root
    password: Kuba1234*
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
    enabled: false